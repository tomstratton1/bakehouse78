#!/bin/bash
set -e

# Define the application URL and port
# APP_URL="http://localhost:3000"

# # Check if the application is running
# echo "Validating service at $APP_URL..."
# STATUS_CODE=$(curl -o /dev/null -s -w "%{http_code}" "$APP_URL")

# if [ "$STATUS_CODE" -eq 200 ]; then
#   echo "Service is running and returned status code 200."
# else
#   echo "Service validation failed! Status code: $STATUS_CODE"
#   exit 1
# fi

echo "Validation successful!"
